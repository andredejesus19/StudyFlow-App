import { Platform } from 'react-native';
import { Button, Image, ImageBackgroundBase, Touch, TouchableOpacity, Linking, StyleSheet, Text, TextInput, TouchableWithoutFeedback, View, ScrollView, } from 'react-native';
import styles from './style';

export default function app() {
  
    return (
        
        <Text style={styles.textTeste}>Teste do Texto</Text>
        
        <View style={styles.contaneirMain}>
        
        </View>
    );

};